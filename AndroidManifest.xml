<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivity"
    android:versionCode="1"
    android:versionName="1.0">

    <application android:icon="@drawable/im" android:label="@string/app_name">
        <activity android:name=".LoginActivity"
            android:label="Login to your Account">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  Entry for RegisterActivity.class -->
        <activity android:name=".RegisterActivity"
            android:label="Register New Account"></activity>

        <activity android:name=".HomeActivity"
            android:label="HomeActivity"></activity>

        <activity android:name=".SingleListItem1"
            android:label="Single Item Selected"></activity>

        <activity android:name=".AndroidListViewActivity1"
            android:label= "Android list"></activity>

        <activity android:name=".MainActivity"
            android:label= "Spinner list"></activity>


    </application>
</manifest>